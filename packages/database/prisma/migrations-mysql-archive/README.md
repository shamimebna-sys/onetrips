# Archived MySQL Prisma migrations

These SQL files were generated for MySQL 8 and are **not** part of the live Prisma migration history.

PostgreSQL uses a new baseline in `prisma/migrations/`. Do not apply this archive with `prisma migrate deploy`.

Keep the files for historical audit of the MySQL schema only.
